version: "3.9"

services:
  llm-router:
    build: .
    container_name: llm_prompt_router
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./route_log.jsonl:/app/route_log.jsonl
    stdin_open: true
    tty: true